# Configuration pour le script de reconnaissance de numéros de voitures

# Clé API Claude (obligatoire)
# Obtenez votre clé sur: https://console.anthropic.com/
CLAUDE_API_KEY=YOUR_API_KEY

# Dossier contenant les photos à analyser (JPEG et RAW)
PHOTOS_FOLDER=./photos

# Fichier de sortie pour les résultats
OUTPUT_FILE=car_numbers_results.txt

# Dossier pour sauver les fichiers RAW convertis en JPEG
CONVERTED_FOLDER=./converted_jpg

# Taille de conversion des fichiers RAW (en pixels, côté le plus long)
CONVERT_RAW_SIZE=1920

# Garder les fichiers JPEG convertis (true/false)
KEEP_CONVERTED=true

# Délai entre les appels API (en secondes) - optionnel
DELAY_BETWEEN_CALLS=1.0

# Taille maximum des images (en pixels) - optionnel
# MAX_IMAGE_SIZE=1920

# Dossier pour les XMP
XMP_FOLDER=/mnt/media/photos/christophe/dxo/a_traiter/eos_r7/LM2025/car_193

# Valeur pour XMP dc:description
XMP_DC_DESC="Ferrari 296 LMGT3 #193 - Ziggo Sport Tempesta"